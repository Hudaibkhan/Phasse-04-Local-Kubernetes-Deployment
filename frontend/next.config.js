/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // Enable standalone output for Docker containerization
  // Add any additional configuration needed
};

module.exports = nextConfig;